{
  "name": "kptc-reporting-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Railway deployment wrapper for the KPTC Reporting Platform API.",
  "scripts": {
    "build": "npm --prefix server ci",
    "start": "npm --prefix server start",
    "migrate": "npm --prefix server run migrate",
    "seed": "npm --prefix server run seed",
    "test": "npm --prefix server test && npm --prefix web run build"
  },
  "engines": {
    "node": ">=20"
  }
}
